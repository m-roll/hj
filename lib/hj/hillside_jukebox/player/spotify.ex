defmodule HillsideJukebox.Player.Spotify do
  def play_track(user = %HillsideJukebox.User{}, song = %HillsideJukebox.Song{platform: :spotify}) do
    # The API client needs to be filled in here. This is just a stupid implementation.
    # PR will be located here: https://github.com/jsncmgs1/spotify_ex
  end
end
